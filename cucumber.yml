default: -p prd 
# -p html
default_local: -p local 
# -p html

# Configurações de reports
# html: --format pretty --format html --out=reports/report.html

# Ambientes para execução
prd: ENVIRONMENT="prd"
local: ENVIRONMENT="local"